<%= render "layouts/flash" %>
<h1><%= @shop.name %>店舗画像管理</h1>
  <%= form_with model: @shop, url: admin_shop_shop_path(@shop), local: true do |form| %>
  <% if @shop.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(@shop.errors.count, t('errors.count')) %> <%= t('errors.prohibited') %>:</h2>
      <ul>
        <% @shop.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= image_upload_field(form, @shop, :shop_logo, "お店ロゴ") %>

  <div class="row mb-3">
    <div class="col-9">
      <%= form.label :image1 %>
      <%= form.file_field :image1, class: "form-control" %>
    </div>
    <div class="col">
      <% if @shop.image1.present? %>
        <%= image_tag(@shop.image1.to_s, class: "img-fluid") %>
        <label>
          <%= form.check_box :remove_image1 %>
          画像削除
        </label>
      <% else %>
        画像１が登録されていません。
      <% end %>
    </div>
  </div>

  <div class="row mb-3">
    <div class="col-9">
      <%= form.label :image2 %>
      <%= form.file_field :image2, class: "form-control" %>
    </div>
    <div class="col">
      <% if @shop.image2.present? %>
        <%= image_tag(@shop.image2.to_s, class: "img-fluid") %>
        <label>
          <%= form.check_box :remove_image2 %>
          画像削除
        </label>
      <% else %>
        画像2が登録されていません。
      <% end %>
    </div>
  </div>

  <div class="row mb-3">
    <div class="col-9">
      <%= form.label :image3 %>
      <%= form.file_field :image3, class: "form-control" %>
    </div>
    <div class="col">
      <% if @shop.image3.present? %>
        <label>
          <%= form.check_box :remove_image3 %>
          画像削除
        </label>
        <%= image_tag(@shop.image1.to_s, class: "img-fluid") %>
      <% else %>
        画像3が登録されていません。
      <% end %>
    </div>
  </div>

  <div class="row mb-3">
    <div class="col-9">
      <%= form.label :image4 %>
      <%= form.file_field :image4, class: "form-control" %>
    </div>
    <div class="col">
      <% if @shop.image4.present? %>
        <label>
          <%= form.check_box :remove_image4 %>
          画像削除
        </label>
        <%= image_tag(@shop.image4.to_s, class: "img-fluid") %>
      <% else %>
        画像4が登録されていません。
      <% end %>
    </div>
  </div>

  <div class="row mb-3">
    <div class="col-9">
      <%= form.label :image5 %>
      <%= form.file_field :image5, class: "form-control" %>
    </div>
    <div class="col">
      <% if @shop.image5.present? %>
        <label>
          <%= form.check_box :remove_image5 %>
          画像削除
        </label>
        <%= image_tag(@shop.image5.to_s, class: "img-fluid") %>
      <% else %>
        画像5が登録されていません。
      <% end %>
    </div>
  </div>

  <div class="actions">
      <%= form.submit t('helpers.submit.create'), class: "btn btn-user w-100" %>
    </div>
  </div>
<% end %>

